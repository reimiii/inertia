import React from 'react';
import App from '../../Layout/App';

const Login = () => {
    return (
        <App title={`Login`}>
            Login Page
        </App>
    );
};

export default Login;
