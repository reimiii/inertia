import React from 'react';
import App from '../Layout/App';

const Dashboard = () => {
    return (
        <App title={`Dashboard`}>
            This is the Dashboard
        </App>
    );
};

export default Dashboard;
